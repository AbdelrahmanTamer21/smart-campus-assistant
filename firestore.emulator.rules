rules_version = '2';
// Open rules for the local Firestore emulator only (see firebase.json).
// Production deploys firestore.rules — never deploy this file.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
